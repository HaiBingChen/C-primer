#include <iostream>
#include <fstream>

using namespace std;

ofstream& open_file(ofstream& out, const tring file)
{
	in.close();
	in.clear();

	out.open(file.c_str());

	return out;
}

int main(void)
{
	return 0;
}
